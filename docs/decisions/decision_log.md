# Decision Log — SOEN 6011 F6 Beta Function

Records every significant decision: alternatives considered, criteria, the decision,
its rationale, the date, and the artifacts it affects. Include rejected options.
(Cross-deliverable task X-02.)

| # | Date | Decision | Alternatives considered | Rationale / criteria | Affected artifacts | Status |
|---|------|----------|-------------------------|----------------------|--------------------|--------|
| D-001 | 2026-07-14 | Scope the supported domain to `x > 0`, `y > 0`. | (a) x>0,y>0; (b) broader real domain via analytic continuation. | Euler integral is finite and real for x,y>0; avoids poles; keeps scope realistic for a medium project. | Requirements, algorithms, error handling, tests | **Proposed — confirm with professor** |
| D-002 | 2026-07-14 | Output a real decimal approximation with a documented tolerance. | Symbolic/exact output. | Consistent with a numerical scientific calculator and real inputs. | Requirements, CLI, tests | Proposed |
| D-003 | 2026-07-14 | Create repo `beta-function-calculator/` with the task-plan directory structure. | Flat layout; per-deliverable repos. | Matches the suggested structure; separates source, docs, deliverables. | Whole project | Done |
| D-004 | 2026-07-15 | **Primary algorithm = Algorithm B** (Gamma identity `Γ(x)Γ(y)/Γ(x+y)` via log-domain Lanczos `lnΓ`). | Algorithm A (Euler adaptive-Simpson quadrature); Algorithm B (Gamma identity). | B is the only candidate meeting ACC-01 uniformly (18/18 reference values, worst rel err 1.2e-14) **and** REL-02 (log-domain, no overflow), REL-03 (fixed loop), PERF-01 (O(1)); A fails ACC-01 near singular endpoints (B(0.2,0.3), rel err 2.7e-3). A rejected for this stage; B's `exp`/`ln`/`sin` become the D2 from-scratch targets. | CLI (`src/cli.py`), D2 core, tests, selection mind map | **Decided (P4)** — see `docs/mindmaps/algorithm_selection.dot` |
| D-005 | 2026-07-14 | Use a **goal-directed persona template**, trimmed to one page, with explicit evidence/assumption labels. | Goal-directed (Cooper); Role-based; Proto/lightweight. | Goal-directed gives the strongest traceability from user goals/pains to requirements; grafted proto's assumption-labelling; role-based rejected for weak goal/pain traceability. | Persona, requirements (P2) | Done — see mind map |
| D-006 | 2026-07-15 | Adopt the 29148 **imperative "shall" statement style** with category-prefixed IDs (FR/VAL/ACC/…). | Use-case style; user-story style; imperative shall style. | Best fit for a small, verifiable scientific tool; each statement singular and testable. | Requirements baseline | Done |
| D-007 | 2026-07-15 | Set accuracy target **relative error ≤ 1×10⁻⁶** over `0 < x,y ≤ 50`; magnitude cap `x,y ≤ 1×10⁴`. | Tighter (1e-9); looser (1e-3); no bound. | Measurable and achievable from-scratch via log-domain evaluation; bound keeps behaviour testable and avoids overflow-region ambiguity. | ACC-01, VAL-04, tests | **Re-validated (D2)** — worst rel err 1.17e-14 on from-scratch build |
| D-008 | 2026-07-24 | **From-scratch boundary:** reimplement only `ln`, `exp`, `sin`, π (and `is_finite`); retain arithmetic, `int`/`float` casts, string formatting, Tkinter, and exceptions as permitted I/O/arithmetic/UI facilities. | (a) reimplement everything incl. parsing; (b) also reimplement `pow`; (c) this scoped boundary. | Matches the Problem 5 rule (input/output/arithmetic/UI exempt); Algorithm B needs no `pow`; casts/formatting are I/O, not mathematics. Recorded as an assumption + README note. | `elementary.py`, `beta_core.py`, boundary doc, IMPL-01 | **Decided** — see `docs/algorithms/d2_from_scratch_notes.md` |
| D-009 | 2026-07-24 | **Elementary-function algorithms:** range-reduction + series for each — atanh series for `ln` (`x=m·2^e`), Taylor for `exp` (`x=k·ln2+r`), argument-reduced Taylor for `sin`. | Pure Taylor (no reduction, slow/inaccurate for large args); CORDIC; minimax/Chebyshev polynomials. | Range reduction keeps every series argument small so ~12–20 terms reach machine precision with simple, auditable code; verified ≤ 6×10⁻¹⁵. Minimax/CORDIC add complexity without needed accuracy gain. | `elementary.py`, elementary-functions doc, ACC-03p | **Decided** |
| D-010 | 2026-07-24 | **GUI:** single resizable Tkinter window (`ttk`), wireframed before coding; two labelled entries, Calculate/Clear/Help, one text status line; keyboard-first. | Multi-window; canvas plot; menu-driven; extra controls. | Matches the persona (one quick value, low CLI comfort); minimal surface reduces error; wireframe fixes layout/traceability before implementation. | `gui.py`, wireframe doc, UI-01 | **Decided** — see `docs/gui_wireframe.md` |
| D-011 | 2026-07-24 | **Typed exception hierarchy** `BetaError` → Input/Domain/Range/Numerical subclasses, each carrying a user-facing `message`. | Bare `ValueError`s with string sniffing; error codes; `(ok, value, msg)` tuples. | Lets the GUI catch categories separately (input vs numerical) while a single `except BetaError` guarantees no traceback (REL-01); keeps helpful messages next to their cause (ERR-01/ERR-02). | `exceptions.py`, `validation.py`, `beta_core.py` | **Decided** |
| D-012 | 2026-07-24 | **Requirements baseline v0.2** — retain all v0.1 IDs; add IMPL-01, UI-01, ERR-02; reword FR/USE/POR for the GUI; promote ACC-03 to must; make REL-02 failure explicit. | Rewrite from scratch; keep v0.1 unchanged. | Problem 7 requires updating v0.1 *based on* the D2 build; preserving IDs keeps traceability; changes are implementation-discovered, each with a rationale. | `requirements_updated_v0.2.md` | **Decided (P7)** |
| D-013 | 2026-07-24 | **Publish to a public Git host with an incremental, high-quality commit history**; keep `cli.py` as the frozen D1 baseline alongside the new D2 modules. | Squash into one commit; delete the D1 CLI; per-deliverable repos. | Problem 6 requires a public repo with high-quality commits + README; incremental history shows evolution; keeping the CLI enables a regression baseline and honest before/after story. | repo, README, CHANGELOG, git history | **Decided** — public URL added by author on push |

> Add a new row for each decision as work progresses. Do not delete rows — mark them
> superseded and add a replacement row so the history stays intact.
